# ==============================================
# BACKEND DEVELOPMENT (if running separately from Docker)
# ==============================================
# Only needed if you're running the backend separately from Docker
# When using Docker, all variables are in the root .env file

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (Local Development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydatabase
DB_USER=myuser
DB_PASSWORD=mypassword
DB_SSL=false

# Security Configuration (Development Keys - CHANGE FOR PRODUCTION!)
JWT_SECRET=dev-jwt-secret-key-12345-change-for-production
ENCRYPTION_KEY=abcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890

# Admin Configuration
DEFAULT_ADMIN_PASSWORD=admin123

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
